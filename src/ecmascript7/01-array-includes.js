let numbers = [1,2,3,4,7,8, ];
console.log(numbers.includes(9));//Esta forma de buscar datos dentro de un array solo me retorna un boolean

const list = ['Oscar', 'David', 'Ana'];
console.log(list.includes('oscar'));